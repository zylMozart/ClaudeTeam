# ── 凭证与密钥（绝不入库）──
.env
.env.*
!.env.example
scripts/.token_cache.json

# ── 运行时配置（绝不入库）──
scripts/runtime_config.json
scripts/sync_manifest.json
scripts/.router_seen_ids.json
scripts/.*.pid
scripts/.router.cursor
scripts/.team.json.prev

# ── 运行时生成的文件与目录 ──
CLAUDE.md
agents/
workspace/
team.json

# ── 数据库 ──
*.db

# ── Python ──
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ── macOS ──
.DS_Store

# ── 编辑器 ──
.vscode/
.idea/
*.swp

# ── 其他 ──
.playwright-mcp/
tools/
docker-compose.override.yml
.kimi-credentials/
.codex-credentials/
.gemini-credentials/
