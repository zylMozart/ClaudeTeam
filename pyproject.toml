[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "claudeteam"
version = "0.1.0"
description = "Harness your Claude Code: multi-agent orchestrator over tmux + Feishu"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
  # stdlib `tomllib` is 3.11+; on 3.9/3.10 we read TOML via the `tomli` backport
  # (runtime/tunables.py + agents/kimi_code.py import it under a try/except).
  'tomli; python_version < "3.11"',
]
keywords = ["claude-code", "agents", "tmux", "feishu", "lark", "llm-orchestration"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Operating System :: POSIX :: Linux",
  "Operating System :: Microsoft :: Windows",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development",
]

[project.urls]
Homepage = "https://github.com/zylMozart/ClaudeTeam"
Repository = "https://github.com/zylMozart/ClaudeTeam"
Issues = "https://github.com/zylMozart/ClaudeTeam/issues"

[project.scripts]
claudeteam = "claudeteam.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"
